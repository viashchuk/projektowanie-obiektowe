{% extends 'base.html.twig' %}

{% block title %}PaymentMethod{% endblock %}

{% block body %}
    <h1>PaymentMethod</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ payment_method.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ payment_method.code }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ payment_method.title }}</td>
            </tr>
            <tr>
                <th>Active</th>
                <td>{{ payment_method.active ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_payment_method_index') }}">back to list</a>

    <a href="{{ path('app_payment_method_edit', {'id': payment_method.id}) }}">edit</a>

    {{ include('payment_method/_delete_form.html.twig') }}
{% endblock %}
